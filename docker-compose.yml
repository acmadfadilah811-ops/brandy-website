services:
  brandy-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: brandy-website
    ports:
      - "3000:3000"
    restart: always
    environment:
      - PORT=3000
